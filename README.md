# Yahoo Fantasy HA

Home Assistant custom_component exposing Yahoo Fantasy NFL score/matchup data
(current week team score, opponent, record, matchup status) as sensor entities.

Installed via HACS as a custom repository. See the kanban board for spec/status.
